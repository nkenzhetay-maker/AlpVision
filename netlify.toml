[build]
  functions = "functions"

[functions]
  node_bundler = "esbuild"

# generate-bg çok uzun sürüyor — max timeout
[functions."generate-bg"]
  timeout = 26

[functions."analyze"]
  timeout = 26

[functions."vision-qc"]
  timeout = 26

[functions."fetch-inspiration"]
  timeout = 10
